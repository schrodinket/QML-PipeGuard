# results

experiment json outputs land here. each file is named
`<experiment>_<backend>_<timestamp>.json`.

this directory is gitignored except for this readme, so
contributors won't accidentally commit raw experimental data.
